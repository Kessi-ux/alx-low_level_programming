#include <stdio.h>
/**
 *main- A c program that displays information to the output
 *Description: It formats and prints exactly: with proper grammar, but the outcome is a piece of art,
 *Return: Returns 0 as success value
 */
int main(void)
{
	printf("\nwith proper grammar, but the outcome is a piece of art,");
	return (0);
}
